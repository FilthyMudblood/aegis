Run from this directory (repository root):

  pip install -r requirements.txt
  streamlit run app.py

For GitHub: use .gitignore at the repo root; proprietary modules live under aegis_private/ and are ignored—no separate public/ snapshot is needed.
